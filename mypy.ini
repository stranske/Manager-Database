[mypy]
ignore_missing_imports = true
explicit_package_bases = true
# Ignore untyped imports for packages without stubs
disable_error_code = import-untyped

# Exclude scripts directory from duplicate module detection
# since scripts/*.py are standalone and also importable as scripts.X
exclude = (?x)(
    ^scripts/board_sync\.py$
  )
