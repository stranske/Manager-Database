# Pin tool versions for consistent CI behavior across all workflows
# These versions should match what is installed in requirements.txt or lock files
#
# Update these versions when upgrading tools to keep CI consistent with local dev.

# Python formatting and linting
BLACK_VERSION=24.10.0
RUFF_VERSION=0.8.4

# Type checking
MYPY_VERSION=1.13.0

# Testing
PYTEST_VERSION=8.3.4
PYTEST_COV_VERSION=6.0.0
COVERAGE_VERSION=7.6.9
