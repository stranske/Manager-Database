[run]
# Adapter coverage runs should include the full adapters package by default.
source =
    adapters
omit =
    ui/*
    # Exclude workflow-only helper that is exercised in Workflows repo CI, not here.
    tools/coverage_trend.py
